package GUI.Scenes;

/**
 * An interface that describes the scene object. This interface is going to remain empty, as it will only be used as
 * a flag to indicate that a class is, indeed, a scene.
 */
public interface IScene {
}
